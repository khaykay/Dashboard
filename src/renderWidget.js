import { useState } from "react";
import { Data } from "./db/db";

export const renderWidget = (type, dataSource) => {
  const [userData, setUserData] = useState({
    labels: [],
    dataSet: [],
  });
};
console.log(Data);
